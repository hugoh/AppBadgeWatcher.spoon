[tools]
"github:orhun/git-cliff" = "latest"
actionlint = "latest"
dprint = "latest"
ghalint = "latest"
gitleaks = "latest"
hk = "latest"
lua = "5.4"
rumdl = "latest"
shellcheck = "latest"
shfmt = "latest"
stylua = "latest"
taplo = "latest"
tombi = "latest"
typos = "latest"
# yamllint = "latest"
zizmor = "latest"

[tasks.package]
run = """
rm -rf dist
mkdir -p dist/AppBadgeWatcher.spoon
cp init.lua LICENSE README.md dist/AppBadgeWatcher.spoon/
cd dist
zip -r ../AppBadgeWatcher.spoon.zip AppBadgeWatcher.spoon
cd ..
rm -rf dist
"""
description = "Build a self-contained AppBadgeWatcher.spoon.zip"

[tasks.test]
run = "busted test_spec.lua --verbose"

[hooks]
postinstall = ["luarocks install busted", "luarocks install luacheck"]

[settings]
experimental = true # for hooks

[env]
HK_PKL_BACKEND = "pklr"
